---
modules:
  blazemeter:
    test: Taurus Demo

scenarios:
  simple:
    requests:
    - label: HomePage
      url: http://blazedemo.com/
    - label: PurchasePage
     url: http://blazedemo.com/purchase.php

services:
- module: passfail
  criteria:
  - avg-rt of HomePage>150ms for 10s, continue as failed
#  - fail of PurchasePage>50% for 10s, stop as failed

execution:
  scenario: simple
  hold-for: 5m
  concurrency: 200
  ramp-up: 60s
